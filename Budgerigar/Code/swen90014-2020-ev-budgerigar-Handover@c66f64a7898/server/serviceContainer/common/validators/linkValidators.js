module.exports = {
    
    validateHansardDocumentExists : async function(link) {
        console.log(`Validating hansard document exists at ${link}`);
        console.warn(`Validation logic is missing to ensure this is an accessible link.`);

        return true;
    }
}